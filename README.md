# Machine Learning for Load Balancer

Repository map
- CockroachDB benchmark in `cdb_bench`
- HAProxy fork in `haproxy`

Load all submodules with
```
git submodule init
git submodule update --recursive
```
